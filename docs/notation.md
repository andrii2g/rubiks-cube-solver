# Notation

Supported MVP moves are `U`, `R`, `F`, `D`, `L`, and `B` with optional `'`, `i`, or `2` suffixes.

Examples:

```text
R U R' U' F2
Ui Ri Fi
```

Slice moves, wide moves, cube rotations, and lowercase notation are not supported in the MVP.
