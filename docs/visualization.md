# Visualization

The visualizer will render a 2D cube net:

```text
      U
L     F     R     B
      D
```

The Blazor UI should reuse Core cube and move logic instead of duplicating move behavior in components.
